export interface userState {
    id: string,
    fn: string,
    ln: string,
    email: string,
    avatar: string,
    dark_mode: boolean,
    username: string,
    wallet: number,
    current_path: string,
    previous_path: string
}